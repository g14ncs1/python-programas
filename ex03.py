#O seguinte programa calcula a área de um triângulo
alt = float(input("Entre com a altura do triângulo "))
bas = float(input("Entre com a base do triângulo "))
#As variáveis foram mudadas para float para que não haja erro ou perda de precisão na multiplicação
print("A área do triângulo dado é:", (alt*bas)/2)